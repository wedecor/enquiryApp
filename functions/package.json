{
  "name": "wedecor-functions",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "clean": "rimraf lib dist node_modules .turbo .parcel-cache .cache || true",
    "build": "tsc -p tsconfig.json",
    "deploy": "firebase deploy --only functions:inviteUser,functions:notifyOnEnquiryChange",
    "lint": "echo \"(optional) add eslint if needed\""
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.4.0",
    "nodemailer": "^6.9.8"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3",
    "@types/nodemailer": "^6.4.14",
    "eslint": "^9.9.0"
  }
}
