{
  "name": "wedecor-functions",
  "private": true,
  "type": "commonjs",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p .",
    "deploy": "firebase deploy --only functions:notifyOnEnquiryChange",
    "lint": "echo \"(optional) add eslint if needed\""
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.0.1"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}